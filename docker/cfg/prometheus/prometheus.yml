global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: proxmox-backups
    scrape_interval: 5m
    metrics_path: /metrics
    static_configs:
    - targets:
        - proxmox-metrics:8080